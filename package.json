{
  "name": "sierra-vault",
  "version": "1.0.0",
  "description": "Blockchain-verified digital vault for Sierra Leone citizens",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend:memory": "cd frontend && set NODE_OPTIONS=--max-old-space-size=4096 && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["blockchain", "digital-vault", "documents", "sierra-leone"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

